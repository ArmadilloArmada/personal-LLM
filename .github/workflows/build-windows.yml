name: Build Persona Windows .exe (deprecated)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Builds moved to ArmadilloArmada/Persona
        run: |
          echo "::error::Windows builds now run from https://github.com/ArmadilloArmada/Persona"
          echo "This repo (personal-LLM) is deprecated. Tag and release from Persona instead."
          exit 1
